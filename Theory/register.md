take some data

validate the data

check in DB if user already exists 

SAVED the new user(Access token, refresh token, generate token, sendmail)

user verification => email

send response back to the request